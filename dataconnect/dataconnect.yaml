specVersion: "v1beta"
serviceId: "Cameo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Cameo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
